[package]
name = "antfarm-core"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
rand = "0.9"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[build-dependencies]
antfarm-tools = { path = "../antfarm-tools" }
anyhow = "1"
